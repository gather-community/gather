<% if @has_activity %>
  <div class="row">
    <div class="col-sm-6">
      <%= render("summary", footnotes: footnotes) %>
      <%= render("statement_list") %>
    </div>
    <div class="col-sm-6">
      <%= render("payment_instructions") %>
    </div>
  </div>
  <%= render("other_accounts") if accounts %>
<% else %>
  <div class="center-notice">
    There is no activity on this account.
  </div>
<% end %>
