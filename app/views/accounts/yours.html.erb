<% content_for(:title, multi_community? ? "Your #{@community.name} Account" : "Your Account") %>

<% if accounts.empty? %>
  There are not yet any charges associated with your household.
<% else %>
  <%= render("synopsis") if @has_activity %>
  <%= render("details", footnotes: true) %>
<% end %>
