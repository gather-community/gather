<% content_for(:flashes, inactive_notice(calendar)) if calendar.inactive? %>
<% content_for(:title, "Edit Calendar: #{calendar.name_with_inactive}") %>
<% content_for(:top_buttons, calendar.action_links(:edit)) %>
<%= render("form") %>
