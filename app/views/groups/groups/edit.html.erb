<% content_for(:flashes, inactive_notice(group)) if group.inactive? %>
<% content_for(:title, "Edit Group: #{group.name}") %>
<% content_for(:action_links, group.action_links(:edit)) %>
<%= render("form") %>
