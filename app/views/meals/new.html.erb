<% content_for(:title){ "Create Meal" } %>

<% missing = [] %>
<% missing << "an applicable cost formula" if @meal.formula.nil? %>
<% missing << "at least one meal-hostable resource" if @resource_options.empty? %>

<% if missing.any? %>
  <div class="none-found">
    Before you can create a meal, <%= missing.join(" and ") %> must exist.
  </div>
<% else %>
  <%= render("form") %>
<% end %>
