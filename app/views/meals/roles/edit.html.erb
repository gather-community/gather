<% content_for(:flashes, inactive_notice(role)) if role.inactive? %>
<% content_for(:title, "Edit Meal Role: #{role.title}") %>
<% content_for(:action_links, role.action_links(:edit)) %>
<%= render("form") %>
