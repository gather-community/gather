<% content_for(:flashes, inactive_notice(type)) if type.inactive? %>
<% content_for(:title, "Edit Meal Type: #{type.name}") %>
<% content_for(:action_links, type.action_links(:edit)) %>
<%= render("form") %>
