<% content_for(:title, "Change Password") %>

<%= gather_form_for(current_user, top_error_notification: false,
                                  width: :narrow,
                                  layout: :vertical,
                                  url: people_password_change_path) do |f| %>
  <%= render("password_fields", f: f) %>
<% end %>
