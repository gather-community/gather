<% content_for(:title, "People Settings") %>

<%= gather_form_for(@community, url: people_settings_path) do |f| %>
  <%= f.fields_for(:settings) do |ff| %>
    <%= render("admin/settings/group_entry", group_entry: @people_settings, f: ff) %>
  <% end %>
  <%= form_actions do %>
    <%= f.button :primary %>
  <% end %>

  <%= javascript_tag do %>
    $(function() {
      new Gather.Views.DirtyChecker({el: '.community-form'});
    });
  <% end %>
<% end %>
