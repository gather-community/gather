<% content_for(:title) { "Protocols" } %>
<h1>Listing reservation protocols</h1>

<table>
  <tr>
    <th>ID</th>
    <th>Req Kind</th>
    <th></th>
  </tr>

  <% @protocols.each do |protocol| %>
  <tr>
    <td><%= protocol.id %></td>
    <td><%= protocol.requires_kind %></td>
    <td><%= protocol.kinds %></td>
  </tr>
  <% end %>
  </table>
