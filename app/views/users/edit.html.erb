<% content_for(:title) do
  if @user == current_user
    "Profile"
  elsif policy(@user).update_info?
    "Edit User: #{@user.name}"
  elsif policy(@user).update_photo?
    "Edit Photo: #{@user.name}"
  end
end %>

<%= inactive_notice(@user) %>
<%= render("form") %>
<br/><br/>
