<div class="center-notice">
  <% if policy(sample_period).create? %>
    <%= t("work.no_periods_html", link: link_to(t("work.periods_page"), work_periods_path)) %>
  <% else %>
    <%= simple_format(t("work.not_setup")) %>
  <% end %>
</div>
