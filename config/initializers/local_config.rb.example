# Google OAuth2 credentials
Devise.setup do |config|
  config.omniauth :google_oauth2, "xxx.apps.googleusercontent.com", "xxx"
end

# generate your secret key with `rake secret`
Mess::Application.config.secret_key_base = "xxx"

Rails.configuration.x.host = "example.com"

Rails.configuration.action_mailer.smtp_settings = {
  address: "example.com",
  port: 587,
  domain: "example.org",
  authentication: :plain,
  user_name: "user",
  password: "xxx"
}
