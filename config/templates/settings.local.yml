# Local development settings
# Generated by bin/setup - safe to customize
#
# This file contains environment-specific settings that should not be committed.
# Use bin/setup to reconfigure, or edit values directly.

# Google OAuth credentials
# Create at: https://console.cloud.google.com/apis/credentials
oauth:
  google:
    client_id:
    client_secret:

# Rails secret key base (auto-generated)
secret_key_base:

# Email settings
email:
  from: Gather <noreply@example.coop>
  webmaster: developer@example.com

# Elasticsearch connection
# Use 'elasticsearch' as host when running in Docker
elasticsearch:
  host:
  port: 9200

# Redis connection
# Use 'redis' as host when running in Docker
redis:
  url:
